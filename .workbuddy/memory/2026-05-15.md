# 2026-05-15 工作日志

## 多用户认证系统 - 集成测试与数据隔离完善

### 完成工作
1. **修复前端构建**：前端dist目录丢失，重新 `npm run build` 成功
2. **启用认证**：将 `.env` 中 `ADMIN_AUTH_ENABLED` 从 `false` 改为 `true`
3. **完善数据隔离**：
   - `HistoryService` 所有方法添加 `user_id` 参数支持
   - `api/v1/endpoints/history.py` 所有端点从 `request` 提取 `user_id` 并传递
   - `StockAnalysisPipeline` 添加 `db_user_id` 参数，传递到 `save_analysis_history`
   - `AnalysisService.analyze_stock` 添加 `db_user_id` 参数
   - `TaskQueue.submit_tasks_batch` 和 `_execute_task` 添加 `db_user_id` 参数
   - `api/v1/endpoints/analysis.py` 从请求中提取 user_id 并传递
   - `AnalysisRepository.save` 添加 `user_id` 参数
4. **集成测试全部通过**：管理员登录、用户注册、权限隔离、数据隔离、密码修改、禁用用户、前端构建

### 未完成
- portfolio/backtest/usage 端点的用户数据隔离（Service层未添加user_id支持）

### 技术细节
- `_resolve_record` 方法增加 `user_id` 过滤：查询记录后检查 `record.user_id == user_id`
- `delete_history_records` 增加用户隔离：先检查所有权再删除
- Pipeline 的 `db_user_id` 独立于 `source_message.user_id`（后者是平台用户ID）

---

## 东方财富妙想 API 数据源替换

### 完成工作
1. **复制 mx-skills 脚本到项目**：`data_provider/mx_api/` 目录，包含4个核心API模块
   - `finance_data.py` — 金融数据查询（searchData）
   - `finance_search.py` — 金融资讯搜索（searchNews）
   - `macro_data.py` — 宏观经济数据查询（searchMacroData）
   - `stocks_screener.py` — 选股/选板块/选基金（selectSecurity）
   - `earnings_review/` — 业绩点评（多步骤调用）

2. **创建 MXFetcher 适配器**：`data_provider/mx_fetcher.py`
   - 实现 `BaseFetcher` 兼容接口，替代所有原有数据源
   - 关键方法：`get_daily_data`, `get_realtime_quote`, `get_stock_name`, `get_chip_distribution`, `get_fundamental_context`, `get_belong_boards`, `get_main_indices`, `get_market_stats`, `get_sector_rankings`, `get_capital_flow_context`
   - 宽格式→长格式K线数据转换（`_wide_dto_list_to_daily_dataframe`）
   - 中文数值单位解析（万股→10000, 亿→100000000）

3. **更新 DataFetcherManager**：`data_provider/base.py`
   - `_init_default_fetchers()` 改为只初始化 MXFetcher
   - 所有公共方法添加 MXFetcher 快速路径（优先使用，不可用时回退原有逻辑）
   - 保留原有 fetcher 导入和回退逻辑作为兜底

4. **替换 SearchService**：`src/search_service.py`
   - 新增 `MXFinanceSearchProvider` 类（最高优先级搜索源）
   - 使用 mx-finance-search API 搜索新闻/研报/公告
   - 在 `SearchService.__init__` 中 `insert(0, mx_provider)` 确保最高优先级

5. **新增 API 端点**：`api/v1/endpoints/mx_data.py`
   - `POST /api/v1/mx/finance-data` — 金融数据查询
   - `POST /api/v1/mx/finance-search` — 金融资讯搜索
   - `POST /api/v1/mx/macro-data` — 宏观经济数据查询
   - `POST /api/v1/mx/stocks-screener` — 选股/选板块/选基金

6. **配置更新**：`.env` 和 `.env.example` 添加 `EM_API_KEY`

### DashScope LLM 配置
- API Key: `sk-sp-97fb89ef81b04cde96886c3a7ab2deec`（管理员配置，所有用户共用）
- Base URL: `https://coding.dashscope.aliyuncs.com/v1`（注意：不是 dashscope.aliyuncs.com，也不是 /compatible-mode 路径）
- 模型: `qwen3.6-plus`
- 渠道配置: `LLM_CHANNELS=dashscope`，`LITELLM_MODEL=openai/qwen3.6-plus`
- 连通测试: ✅ 通过

### API 关键参数
- API Base: `https://ai-saas.eastmoney.com`
- API Key: `EM_API_KEY`（默认值: `em_twy8iqm1jeU4skJvkc3P6GrMz7r8Aiou`）
- 请求方式: POST JSON + `em_api_key` header
- 4个端点: `/searchData`, `/searchNews`, `/searchMacroData`, `/selectSecurity`

### Bug 修复（分析流程）
1. **chip_data DataFrame 布尔判断**：`pipeline.py` 中3处 `if chip_data:` 改为 `if chip_data is not None:`，`mx_fetcher.py` 兜底不再返回 DataFrame
2. **数据库缺列**：`conversation_messages`、`llm_usage`、`analysis_history`、`backtest_results`、`backtest_summaries`、`news_intel`、`portfolio_accounts` 7张表通过 `ALTER TABLE ADD COLUMN user_id` 补齐
3. **MXFetcher 日线查询优化**：
   - 查询措辞改为 `{code} 日线行情 最近N天`（API 返回日度宽格式 DTO，含完整 OHLCV）
   - `_wide_dto_list_to_daily_dataframe` 重写：按 DTO 的日度日期数+OHLCV 完整度排序，核心指标优先于"区间"前缀指标
   - 600000 从只返回1行→41行，600519/300750 同步改善
4. **MXFetcher chip_data 兜底**：`_parse_chip_from_dto` 不再返回 DataFrame，统一返回 None

### 已知限制
- mx-finance-data 返回 K 线数据为宽格式（指标在行，日期在列），需 pivot 转换
- 不同查询措辞可能返回不同 DTO 结构，数据完整性（如 high/low 字段）可能不一致
- 原有 fetcher（akshare/tushare 等）文件保留但默认不使用
